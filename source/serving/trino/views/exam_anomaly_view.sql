CREATE OR REPLACE VIEW delta.mooc.exam_anomaly_view AS
SELECT
    event_date,
    course_id,
    exam_id,
    exam_content_id,
    exam_name,
    attempt_id,
    attempt_user_id,
    user_id,
    username,
    time_limit_mins,
    allowed_time_limit_mins,
    exam_is_proctored,
    exam_is_practice_exam,
    exam_is_active,
    attempt_started_at,
    attempt_completed_at,
    attempt_window_start,
    attempt_window_end,
    attempt_duration_secs,
    event_count,
    distinct_sessions,
    distinct_exam_ips,
    created_count,
    started_count,
    ready_to_submit_count,
    submitted_count,
    max_elapsed_time_secs,
    security_event_count,
    distinct_security_sessions,
    distinct_login_ips,
    login_event_count,
    proctoring_event_count,
    first_time,
    last_time,
    last_event_time,
    rolling_mean_7,
    rolling_std_7,
    z_score,
    is_anomaly,
    anomaly_domain,
    anomaly_type,
    entity_type,
    entity_id,
    metric_name,
    metric_value
FROM delta.mooc.exam_integrity_signals;
