# producer

Producer phát **raw logs** vào Kafka theo timestamp để mô phỏng streaming.
Clean và route sang topic phân loại được tách ra Spark job riêng.

Thư mục:
- `src/producer/`: source code.
- Python runtime dùng `pyproject.toml` ở root repo: `/home/cuong/Desktop/DATN/pyproject.toml`.

