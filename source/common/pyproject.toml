[project]
name = "lsp-common"
version = "0.1.0"
description = "Common helpers for log-streaming-platform"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["lsp_common"]

